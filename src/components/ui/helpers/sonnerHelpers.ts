/**
 * Helper module for sonner toast utilities.
 * Exporting non-component helpers from a separate module avoids
 * react-refresh "only-export-components" warnings when co-located with components.
 */

import { toast } from "sonner";

export { toast };
