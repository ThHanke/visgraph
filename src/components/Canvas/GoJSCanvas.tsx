import React from 'react';

/**
 * Canvas removed — legacy -based canvas has been replaced by ReactFlowCanvas.
 * This lightweight stub remains to avoid accidental import/runtime errors in places
 * that may still reference the module. It renders nothing.
 *
 * If you want to fully delete this file from the repo, remove this file after
 * ensuring no remaining imports reference it.
 */

const Canvas: React.FC = () => {
  return null;
};

export default Canvas;
